var GlobalData = {
    serialActive: false,
    deactivateCommand: 'd',
    packageSize: 3
};

let portSettings = {
    timeout: 200,
    isActive: false
}

let SensorsData = [0, 0, 0];

let Log = {
    inactiveMsg: "SerialPort Inactive."
}